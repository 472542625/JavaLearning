x=10;//原子性，直接写入当前线程的内存中，立刻刷新到主内存中
y=x;//非原子性，第一步先读取x的值，第二步将y的值写入当前线程的内存中，因为过程中可能被其他因素打断，两个操作一起就不是原子操作了
x++;//非原子性，第一步读取x的值，第二步x+1，第三步将x的值写入当前线程的内存中
x=x+1;


因此java内存模型只保证了单个操作是原子性，但是几个操作一起就不是原子操作了，需要加synchronized和lock实现该代码块的原子性
